/**
 * Just a test file to see if your
 * setup will create an JFrame app.
 * If you run this, all you will see is a tiny
 * menu bar with close, minimize and max buttons.
 * Normally don't open this and run.
 */
import javax.swing.JFrame;

public class SimpleGUIRunner
{
	public static void main(String[] args)
	{
		JFrame sample = new JFrame();
		sample.setVisible(true);
	}

}
